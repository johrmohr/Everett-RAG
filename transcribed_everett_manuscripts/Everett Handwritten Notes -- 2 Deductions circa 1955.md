# Everett Handwritten Notes -- 2 Deductions circa 1955.pdf

## §2 Deductions 

In this section we restrict ourselves to
precise, not theoretical, observations. This has
the consequence that the final observer states \(\psi_{i}\) are
orthogonal for each object \((x_0, y_0, z_0) = 0\) for i ≠ j. 

A summary around
Since this measurement is imperfect, the representation 

of the line will not converge to the case of
perfectly precise observation, since in the latter case nearly constant
and the system state will to the observer state are nearly eigenstates
(or conversely, in case of dispersion in system eigenstates, the
appropriate observer states nearly orthogonal). 

## §3 §5) At this point the English language becomes
plagiarism clumsy, whereas before the hold a single
note for the observer, afterwards we hold a number
of different notes, all occurring in a superposition, each
of these notes states is a state on observer, so we
can speak of the different observers described by these
notes. 

notes for the observer, afterwards we hold a number
of different notes, all occurring in a superposition. Each
of these notes states is a state on observer, so we
can speak of the different observer described by these
notes. On the other hand, at some physical system
involved, and the same observer is this, so different
states for different elements of the superposition. 

This is the
notion of the
observer.

---

This definition corresponds to the negative of the entropy of a probability distribution and defined by Shannon [7]. 

The positive information is non-positive is not
liability, since it will be proved by induction in we are
different information, when we compare two distributions
actually
allow interested in stochastic information of a distribution,
only in difference. 

add : 

U §2 pg.5, addendum that a good decision of
information theory is to be found in Shannon [7]
on Woodworth [1]. Not, however, that in the theory
of communication one defines information as a
state \(x_i\), which has a priori probability \(\alpha_i\). To
be the \(p_i\). We prefer, however, to regard information
as referring to the information of the distribution itself.
Our information is thus simply the expectation of
the information contained in the state. 

add to pg.10
\[
P = \sum w_n [\gamma_n]
\]

and in Bayes' theorem
\[
P = \sum w_n [\gamma_n] \beta_n = \sum w_n (\beta_n [\gamma_n] \beta_n^{-1})
\]

\[
\begin{align*}
P &= \sum w_n (\beta_n [\gamma_n] \beta_n^{-1}) \\
&= \sum w_n (\beta_n [\gamma_n] \beta_n^{-1}) \gamma_n \\
&= \sum w_n (\gamma_n \beta_n) (\gamma_n \beta_n^{-1}) \gamma_n \\
&= \sum w_n (\gamma_n \alpha_n) (\gamma_n \beta_n^{-1}) \gamma_n
\end{align*}
\]

\(f(AU^{-1}) = Uf(A)U^{-1}\) \(QED\) von Neumann 388