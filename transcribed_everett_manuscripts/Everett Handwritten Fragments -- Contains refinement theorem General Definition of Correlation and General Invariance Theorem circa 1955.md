# Everett Handwritten Fragments -- Contains refinement theorem General Definition of Correlation and General Invariance Theorem circa 1955.pdf

Continuum Distribution: \(\mathcal{D}\)  
\(\mathcal{D}\)  
\(\mathcal{D}\)  
\(\mathcal{D}\)  
\(\mathcal{D}\)  
Continuum Distribution: \(\mathcal{D}\)  
\(\mathcal{D}\)  
\(\mathcal{D}\)  

\(\mathcal{D}\)  
\(\mathcal{D}\)  
\(\mathcal{D}\)  
\(\mathcal{D}\)  

\(\mathcal{D}\)  
Continuum Distribution: \(\mathcal{D}\)  
\(\mathcal{D}\)  

\(\mathcal{D}\)  
\(\mathcal{D}\)  
Continuum Distribution: \(\mathcal{D}\)  

\(\mathcal{D}\)  
\(\mathcal{D}\)  
\(\mathcal{D}\)  
Continuum Distribution \(\mathcal{D}\)  

\(\mathcal{D}\)  
\(\mathcal{D}\)  
\(\mathcal{D}\)  

\(\mathcal{D}\)  

\(\mathcal{D}\)  
\(\mathcal{D}\)  
\(\mathcal{D}\)  

Continuum Distribution: \(\mathcal{D}\)  
\(\mathcal{D}\)  

\(\mathcal{D}\)  

\(\mathcal{D}\)  
Continuum Distribution: \(\mathcal{D}\)  

\(\mathcal{D}\)  

\(\mathcal{D}\)  
\(\mathcal{D}\)  

\(\mathcal{D}\)  

\(\mathcal{D}\)  

\(\mathcal{D}\)  

\(\mathcal{D}\)  
\(\mathcal{D}\)  

Continuum Distribution: \(\mathcal{D}\)  

\(\mathcal{D}\)  

\(\mathcal{D}\)  

\(\mathcal{D}\)  

Continuum Distribution: \(\mathcal{D}\)  

\(\mathcal{D}\)  
\(\mathcal{D}\)  

\(\mathcal{D}\)  

Continuum Distribution: \(\mathcal{D}\)  
\(\mathcal{D}\)  
\(\mathcal{D}\)

---

while \(h \to -\infty\). On the other hand, if we define \(\tilde{T}_x = I_x - h \cdot I\) so to speak, continuity for the effect of subdivision of each stage, we obviously have 

\[ \lim_{\epsilon \to 0} \tilde{T}_x = \int P(x) h \cdot P(x) dx = \tilde{T}_x \]

which we shall define as the "information of the continuous distribution \(P(x)\). While the definition may seem somewhat arbitrary, we would like to point out that Information itself is not only important, but only consequences, or differences of information between two distributions have real significance. Thus, if we have two distributions \(P_1(x)\) and \(P_2(x)\) 

\[ \begin{aligned} \Theta \tilde{T}_x^1 \tilde{T}_x^2 &= \lim_{t \to 0} \left[ \tilde{T}_x^1 - \tilde{T}_x^2 \right] = \lim_{t \to 0} \left[ \tilde{T}_x^1 \cdot h \cdot I - \tilde{T}_x^2 \cdot h \cdot I \right] \\ &= \lim_{t \to 0} \left[ \tilde{T}_x^2 \cdot h \cdot I - \tilde{T}_x^1 \cdot h \cdot I \right] \end{aligned} \]

So that for comparison this definition of information gives the same definition of information of two distributions as also the limit of the difference of information of two discrete approximations.

---

Billions of dollars, given \(P_n^{(x)}(x)\), \(P_n^{(x)}(x)\), subdivide
infinitesimal length \(l\), \(P_n^{(x)} = \int_{m_l}^{(m_l)l} P_n^{(x)}(x) dx\) \(P_n^{(x)} = \int_{m_l}^{(m_l)l} P^{(x)}(x) dx\) 

\[I_n^4 - I_n^2 = \sum_m (P_n^{(x)} \ln P_n^{(x)}) - (P_n^{(x)} \ln P_n^{(x)})\]

\[= \sum_m \left( \int_{m_l}^{(m_l)l} P_n^{(x)} dx \ln \frac{P_n^{(x)}}{P_n^{(x)}} - \int_{m_l}^{(m_l)l} P_n^{(x)} dx \ln P_n^{(x)} \right) \quad \text{as } l \to 0 \quad \text{and } l \to 0 \quad \text{as } l \to 0 \quad \text{and } P_n^{(x)} \text{ is continuous.}\]

\[= \sum_m \left( \int_{m_l}^{(m_l)} P_n^{(x)} dx \ln \frac{P_n^{(x)}}{P_n^{(1)}} - \int_{m_l}^{(m_l)} P_n^{(x)} dx \ln P_n^{(1)} \right) \quad \text{as } l \to 0 \quad \int_{m_l}^{(m_l)} P_n^{(x)} dx \ln P_n^{(\frac{1}{2})} \quad \text{as } l \to 0 \quad \text{and } \int_{m_l}^{(m_l)} P_n^{(x)} dx \ln P_n^{\frac{1}{2}} \quad \text{as } l \to 0 \quad \text{and } (m_l) \to 0 \quad \text{and } (m_l) \to 0\]

\[(con lo que siempre negativos y coincidir diferencias de 1 al diente apropiado, siempre si pasa a cero.)\]

---

We wish to prove that given certainty the
effect upon the condition of identifying value of
random variable \(X\) of quality \(Y\) is distributed between
two values. Let \(P_{ij} = P(X = x_i, Y = y_j)\) be a random
variable distribution, and suppose that we choose to
regard the occurrence of \(X\) on \(x_i\) as a single event \(X_i\). 

Thus our new distribution 

\[P'_{ij} = \begin{cases} P_{ij} & \text{for } i \neq 0, 0 \leq j \leq 2 \\ P_{ij} + P_{ij} & \text{for } i = 0 \\ \text{undefined} & \text{for } i = 1, 0 \leq j \leq 2 \end{cases}\]

\[P'_j = P_j, \quad P'_i = P_i, \quad i = 3, 4, \dots\]

Then
\[\{X \mid X - \{X, Y\} = \sum_{i=1}^{3} P'_{ij} \ln \frac{P'_{ij}}{P_i P_j} - \sum_{i \neq j} P_{ij} \ln \frac{P'_{ij}}{P_i P_j}\}\]

\[= \sum_{i=3, j=1}^{3} P_{ij} \ln \frac{P'_{ij}}{P_i P_j} + \sum_{i \neq j} P_{ij} \ln \frac{P'_{i j}}{P_i P_j} - \sum_{i \neq j} P_{ij} (\ln \frac{P'_{ij}}{P_i P_j} + P_i \ln \frac{P'_{ij}}{P_i P_j}) - \sum_{i \neq j} P_{ij} (\ln \frac{P'}{P_i P_j} + P_i \ln \frac{P'}{P_i P_j})\]

\[= \sum_{j} (P_{ij} + P_{ij}) \ln \frac{P_{ij} + P_{ij}}{(P_i + P_j) P_j} - \sum_{j} P_{ij} \ln \frac{P_{ij}}{P_i P_j} + \sum_{j} P_{ij} \ln \frac{P_{ij}}{P_i \cdot P_j}\]

\[\leq 0 \quad \text{by Lemma 1}\]

other \(\{X, Y\} \leq \{X, Y\}\) and distribution
induce condition

---

Proof of inequality 

\[a_i, b_i > 0\]

\[\text{Remark} \quad \left( \sum_i b_i \right) \ln \left( \frac{\sum_i b_i}{\sum_i a_i} \right) \le \sum_i b_i \ln \frac{b_i}{a_i}\]

Proof, let \(P_i = \frac{a_i}{\sum_i a_i}\) no this \(P_i \ge 0, \sum_i P_i = 1\) 

\[also \quad \sum_i P_i \left( \frac{b_i}{a_i} \right) = \sum_i \frac{a_i}{a_i} \frac{b_i}{a_i} = \sum_i \frac{b_i}{\sum_i a_i}\]

But by Theorem (2) on pointwise (any convex) 

\[\left( \sum_i P_i \left( \frac{b_i}{a_i} \right) \ln \left( \frac{\sum_i P_i \left( \frac{b_i}{a_i} \right)}{\sum_i P_i \left( \frac{b_i}{a_i} \right)} \right) \right) \le \sum_i P_i \left( \frac{b_i}{a_i} \right) (\ln \left( \frac{b_i}{a_i} \right))\]

\[\Rightarrow \quad \left( \frac{\sum_i b_i}{\sum_i a_i} \right) (\ln \left( \frac{\sum_i b_i}{\sum_i a_i} \right)) \le \sum_i \frac{a_i}{a_i} (\ln \left( \frac{b_i}{a_i} \right))\]

\[\text{obtains here}\]

---

And we have proved that any refinement of a
dictionary joint distribution, by which we mean a binary
a value of a random variable, into a multiple of value
new derive the correlation. This circumstance
allows us to give a rigorous definition of correlation
which will apply to joint probability distributions over
completely arbitrary sets, regardless of cardinality, i.e.
for any probability measure over an arbitrary product
space, in the following simple manner. 

Given two arbitrary sets \(X\) and \(Y\), and a
probability measure over the product space \(M_p\{X \times Y\}\),
we consider a (joint) partition \(P\) of \(X\) into measurable sets \(X_1\)
and \(Y_1\) into \(Y_2\), for which \(M_p\) leads to a discrete
probability distribution, with correlation \(\mathbb{E}[X_1 Y_2]^P\). We
must consider a sequence of partitions \(P_1, P_2, \ldots\)
such that each succeeding one is a refinement of the
preceding, and that the marginal probability measure
of any subset is a partition for to zero as \(P \to \infty\). 

By Theorem (2) \(\mathbb{E}[X_1 Y_2]^P \ge \mathbb{E}[X_1]^P \mathbb{E}[Y_2]^P\) if \(m \ge n\). 

that the sequence \(\{\mathbb{E}[X_1 Y_2]^P\}\) is monotone increasing with \(m\),
since always possess a limit (pseudo), which we define
as the correlation \(\{\mathbb{E}[X_1 Y_2]\}\). 

(2) \(\{\mathbb{E}[X_1 Y_2]\} = \lim_{P \to \infty} \{\mathbb{E}[X_1]^P\} = \sup_P \{\mathbb{E}[X_1 Y_2]^P\}\) 

(Note) The limit for any sequence of absolutely converging to 1.
(What's this in the for any given \(P\) there is a
refinement of \(P\), and that \(P'\) is arbitrarily
close to \(\sup_P \{\mathbb{E}[X_1 Y_2]\}\).

---

General
Invariance of Correlation 

Let \(X, Y\) be sets with probability measure \(M_P(X, Y)\) on the product space. 

Let \(f(X)\) be a one-to-one mapping of \(X\) onto \(U\) and \(g(Y)\) be a one-to-one mapping of \(Y\) onto \(V\). 

Then the probability measure \(M_P\) produced on the product space \(U \times V\) is
\[
P(U, V, y) = P(f^{-1}(y), g^{-1}(V, y))
\] 

Consider any partition \(P\) of \(X, Y\) into \(S(X, S)\) and \(S(Y, S)\) which has distribution \(P_{ij}\). The corresponding

---

This theorem, **Preceding**
apply equally to all the
Corollator Branches!

---

Speciality to continue real variable \(y, y\)
with continuous distribution \(p(y)\)