# Everett Handwritten Draft -- 2 Introduction of Observers circa 1955.pdf

## 2. Introduction of Obituaries 

We will now to note obituary about the appearance of phenomena on a subject's level to observe which are briefed within the theory. In order to accomplish this it is necessary to identify some properties of such an observer (state) with subjective knowledge (i.e. perception). Thus in order to note statement to the effect that an observer 0 has observed the result of its necessary that the state of the observer has become changed from its former state to a new state which is dependent upon it. 

It will suffice for our purpose to consider an observer to possess memories (i.e. parts) whose states are in correspondence with past experience of the observer, of a relatively permanent nature. In order to make obituaries about the experience of such an observer it is sufficient to examine the contents of state the memory. The main point that has to be remembered is the connections between these memories.

---

We note that if we consider the current memory
data, we will use machine configuration, is immobility
recorded in the memory, then the actions of its machine
data given instant can be regarded as a function of the
memory content only, and all relevant experience
of the machine is contained in the Memory. For
such machine we are justified in using phrases
"machine" "the machine has perceived A" or
"the machine is aware of A" if the occurrence of A
is represented in the memory, since the future
behaviors of the machine will be based upon the
occurrence of A. In fact, all of the customary
language of subjective experience is quite applicable to
such machines and forms the most natural and
useful mode of expression when dealing with their
behavior, as is well known to individuals who
work with complex automata.
On the contrary with "machine" "the
quantum mechanically we shall ascribe state
functions to them using the notation that 

\[
E = \mathcal{B} \cdot C \cdot \mathcal{T}
\]

stocks for a state function which describes an observer \( \mathcal{O} \),
where memory definitively contains the sequence (within
we shall assume to be realized this value) ... \( A, B, \ldots, S, \ldots \)
(although the symbols \( A, B, \ldots \) stand for
memory configurations which are in correspondence
with part of the universe. Both these configurations
can be thought of as instantaneous possible in a given time,
implying on a microscopic level, configuration of a truly

---

putatively correct, or even configurational or brain cells, the only regime that truly describes the interpretation as "the observables that spanned the succession of events" \(A, B, \ldots, C, \ldots\) is thus "our problem" to treat the interaction of such systems with other physical systems (observation) constitutes the provenance of wave mechanics, tied to deduce the reality memory configuration, which we can then interpret as the subjective experience of the observed. We begin by defining what shall constitute a "good" observation. A "good" observation of a quantity \(A\), with its eigenfunctions \(\phi_i\) for a system \(S\), by an observer whose initial state is \(\psi_0\) [1.7], shall consist of an interaction which, in a specified procedure, transforms \(\phi_i\) (total) state: \(\phi_i \psi_0\) into \(\phi_i\) state (in an arbitrary state). \(\phi_i \psi_0\) into \(\phi_i\) state (in an arbitrary state).

\[\phi_i \psi_0 \rightarrow \phi_i \psi_0\]

\[\phi_i \psi_0 \rightarrow \phi_i \psi_0\]

\[\phi_1 \psi_0 \rightarrow \phi_1 \psi_0\]

\[\phi_1 \psi_0 \rightarrow \phi_1\]

\[\phi_1 \psi_0 \rightarrow \phi_1\]

\[\phi_2 \psi_0 \rightarrow \phi_2 \psi_0\]

\[\phi_2 \psi_0 \rightarrow \phi_2 \psi_0\]

---

0

0 

0
0

<|det|>[[0, 0, 0, 0]]s
0

<|det|>[[0, 0, 0, 0]]is
0

<|det|>[[0, 0, 0, 0]]0

<|det|>[[0, 0, 0, 0]]0
0

<|det|>[[0, 0, 0, 0]0

<|det|>[[0, 0, 0, 0]0
0

<|det|>[[0, 0, 0, 0]

<|det|>[[0, 0, 0, 0]0

<|det|>[[0, 0, 0, 0]0

The first deletion state can make its result of an observation upon a system which is not in an eigenstate of the observation. Since we know, by our previous remark upon what constitutes a good observation that the interaction transforms states \(\psi_{i}^{0} \psi_{i}^{0}\) into \(\psi_{i}^{0} \psi_{i}^{0}\), we can simply superpose these solutions of the wave equation to arrive at the final state of the case of an initial system state \(\psi_{i}^{0} \psi_{i}^{0}\) to get:

<|det|>[[0, 0, 0, 0]]0

partial
0

partial
0

partial
0

0
0

partial
0

partial
0

<|ref|>text
0

<|ref|>text
0

<|ref|>text
0

<|ref|>text
0

<|det|>[[0, 0, 0, 0]0

partial
0

<|ref|>text
0

<|ref|>text
0

The main two also in the presence of further systems which do not interact for the limit of measurement thus is systems \(S_{1}, S_{2}, \ldots , S_{n}\) are present as well as \(O\), (with original states \(\psi_{1} \psi_{2} \ldots \psi_{n}\)), and also the only interaction during the time of measurement is between \(S_{1}\) and \(S_{2}\) the result of the measurement will be the template of the total state:

<|det|>[[0, 0, 0, 0]0

<|det|>[[0, 0, 0, 0]0

---

Thus we arrive at the general rule for the
Transformation of Total State Functions which describe systems
within which observation procedure occurs: 

**Rule 1** The observation of a quantity \(A\) with
eigenfunctions \(\phi_i\) in a system \(S\) by the
observer \(O\), transforms this total state exactly
to:
\[
\gamma^S \psi^S, \gamma^S \psi^O \rightarrow \sum_i a_i \phi_i \gamma^S \psi^S, \gamma^S \psi^O
\]

when \(\gamma^S \sum_i a_i \phi_i = 0\). 

If we next consider a second observation to be
made, when our total state is now a superposition,
we can apply rule 1 separately to each column
of this superposition since each column separately
depends on the equation and behaviour independently of the
remaining elements, and then apply the
results to obtain the final relation. We formulate
this as 

**Rule 2** Rule one may be applied separately to
to each element of a superposition of total system
states, the results being superimposed to obtain the
final total state. Thus a determination of
\(B\) with eigenfunction \(\psi^S\) on \(S_2\) by the observer
\(O\) transforms the total state 

\[
\sum_i a_i \phi_i \gamma^S \psi^S, \gamma^S = \sum_i \gamma^S \psi^O
\]

into the state
\[
\sum_i a_i b_i \phi_i \gamma^S \psi^S, \gamma^S = \gamma^S \psi^O
\]

where \(\gamma^S = \sum_i b_i \gamma^S\).

---

which follow from the application of the
to each element \(x_1, x_2, \ldots, x_n, y_1, y_2, \ldots, y_n\) and
then superposing the resultants the coefficients \(a_1, a_2, \ldots, a_n\). 

These two rules, which follow directly from
the superposition principle, allow us a given a
a convenient method for determining final
total states for any number of observations in any
conjunction. We must now seek the interpretation
of such final total states. 

We first consider the simple case of a single
observation of a quantity \(A\), with eigenfunction \(\psi\), in
the system \(S\) subject to \(x_1, y_1\) by an observer \(O\)
which consists in \(x_1, y_1\). Thereafter, as we have seen,
the superposition of (2.1): 

\[
\begin{equation}
\mathcal{V}_{S=0}^{S=0} = \sum_i a_i \mathcal{V}_i^{0} \mathcal{V}_{(x_1, y_1)}^{0}
\end{equation}
\]

---

We note that their is no longer any independent system state or observation state, although the two have become correlated in an one manner. Each element of the superposition (2,3) \(\sum_{i=1}^{n} \psi_{i}^{0}\) (2,3) identifies a definite system state which is an eigenstate of A and furthermore identifies an observer state definitely perceives that particular system state. This this correlation which allows that to maintain the interpretation that a measurement has been performed.

We carry the discussion a step further and allow the observer to repeat the observation. Then accordingly to rule two we arrive at the total state of the second observation:

---

\[(2.43) \quad \mathcal{V}^{s-r_0} = \sum_i a_i \mathcal{V}_i^{r_0} \quad \mathcal{V}^{r_0} \quad \mathcal{V}^{r_0} \quad \mathcal{V} \quad \mathcal{V} \quad \mathcal{V} \quad \mathcal{V} = \mathcal{V} \quad \mathcal{V} \quad \mathcal{V} \quad \text{and} \quad \mathcal{V} \quad \mathcal{V} \quad \mathcal{V} - \mathcal{V} \quad \mathcal{V} \quad \mathcal{V} \quad \sum_i a_i \mathcal{V}_i^{r_0} \quad \mathcal{I} \quad \mathcal{I} \quad \mathcal{I} \quad \mathcal{I} - \mathcal{I} \quad \mathcal{I} \quad \mathcal{I} \quad \sum_i a_i \mathcal{V}_i^{r_0} \]

Again, we see that each element of (2.4) \(P_i \mathcal{V}^{r_0}\) describes a system eigenstate, each but this also describes a state on observer who has obtained the same result for each of the two observations. Thus to each separate observer which is described by an element of the superposition (2.2) the observation was repeatable. This repeatability is a consequence of the fact that after an observation the relative system state for a particular observer state is the corresponding eigenstate.

---

each of which identifies an observer with a
definite memory sequence \([\alpha_{i}^{1} \alpha_{j}^{2} \ldots \alpha_{k}^{r}]\) and
relative to whom the (observed) system state are
the corresponding eigenfunctions \(\phi_{i}^{1} \phi_{j}^{2} \ldots \phi_{k}^{r}\), the
meaning system Sij ... Sn being unaltered. 

In the language of subjective experience, the
observer which is described by a typical element \(y_{i}^{1} \ldots y_{i}^{k}\)
the superposition, has perceived an opportunity random
sequence of definite results for the observation. Of its
furthermore true that if any of the observers described
by such an element of the superposition should repeat
any one of the preceding discriminative fluctuations
would obtain the same result that was obtained for
the scalar observation \(y_{i}^{1} \ldots y_{i}^{k}\) if at this stage
a reformation of an earlier system observation \(S_{i}\) (S_{i})
this place, every element of the resulting superposition
will describe an observer whose memory state is of the
form \([\ldots \alpha_{i}^{1} \ldots \alpha_{k}^{r} \alpha_{i}^{1}]\) in which the earlier
memory coincide with the later — i.e., the memory
states are annulled. At will this appear to
the observer which is described by a typical element
of the superposition that each initial observation over
a system caused the system to "jump" into an eigenstate
in a random fashion and therefore remain there. However,
qualitatively this probabilistic assertion of process I
appears to be valid to the observer described by a
typical element of the superposition. 

In order to establish quantitative results,
we must put some sort of measure (weighting)
on the element of a final superposition. This is necessary

---

To be able to make assertions which will hold
for element all of the observables identified by
elements of a superposition, in order to make
quantitative statements about the relative
frequencies of the different possible results of
observation which are recorded in the memory
of a typical observer, we must be sure a method
of selecting a typical observer. 

We choose for this measure the square amplitude
of the coefficients of the superposition, a choice
which is in all probability one just as
arbitrary as it appears. This choice then assigns
to the \( \psi_j \) the element of the superposition (2.6) 

\[ \phi_1 \phi_2 \phi_3 \ldots \phi_n \psi_1 \psi_2 \psi_3 \ldots \psi_m \psi_0 \quad [\alpha_1^1 \alpha_2^2 \ldots \alpha_k^r] \]

the mixture (single) 

\[ M_{ij \ldots k} = (a_i a_j \ldots a_k)^* (a_i a_j \ldots a_k) \]

so that the observer, whose memory state is
\( [\alpha_1^1 \alpha_2^2 \ldots \alpha_k^1] \) is assigned the measure
\( \alpha_i^1 \alpha_j^2 \ldots \alpha_k^r \cdot M_{ij \ldots k} \). We substitute with
this a product matrix, namely 

\[ M_{ij \ldots k} = M_1 M_2 \ldots M_k \quad \text{where } M_j = a_j^* a_j \]

---

so that the measure assigned to a particular
memory sequence \([\alpha_1^1, \alpha_2^2, \ldots, \alpha_r^r]\) is simply
the product of the measures \(M_1 = \alpha_1^1\) for the
individual components of the memory sequence. 

We note in our a priori independence of our measure structure
to the probability theory of random sequences. Namely,
if we were to regard the \(M_i\)'s as probabilities
in the sequence \([\alpha_1^1, \alpha_2^2, \ldots, a_r^r]\) then the sequences
are equivalent to those sequences which are
generated by arithmetical operations on the
independent probability \(M_i = \alpha_i^1\). Now
probability theory is equivalent to measure theory
mathematically, so that we can make use of it,
while keeping in mind that all results should
be translatable back to measure theoretic language. 

Thus, in particular, if we consider
the sequences to become longer and longer
(more and more observation performed) each
memory sequence of this final superposition
will satisfy any given criterion for a
randomly generated sequence, generatable by
the independent probability \(a_i^1\), except
for a set of that measure which tends
toward zero as the number of observations
becomes unlimited. (All examples of functions occurring
in the limit almost all
memory sequences satisfy generatable
for random sequences generated with probabilities of
\(a_i^1\).) 

Therefore, in the limit almost all
memory sequences satisfy generatable
for random sequences generated with probabilities of
\(\alpha_i^1\). We can thus say that the statistical equations
of process 1 will appear to be valid to almost
all observers.

---

While we have no far- combined only sequences of
generations of the same quantity upon initial systems,
there will be equally true for arbitrary sequences of
generations. For example, the sequence of generations
of the quantities \( A^4, A^2, \ldots, A^n \), with (generally different)
sign functions as \( \{\phi_1^4, \phi_2^2, \ldots, \phi_n^k\} \) applied
successively to the systems \( S_1, S_2, \ldots, S_n \), with (arbitrary)
initial states \( \psi_1^S, \psi_2^S, \ldots, \psi_n^S \), transforms the
total initial state: 

\[ (2.11) \quad \psi^{-S_1+S_2+\cdots+S_n+\cdots+0} = \psi^{-S_1} \psi^{-S_2} \cdots \psi^{-S_n} \quad \text{by rules one and two, into the final state} \]

\[ (2.12) \quad \psi^{-S_1+S_2+\cdots+S_n+0} = \sum_{i,j,\ldots,k} (\phi_i^1 \psi^2)(\phi_j^2 \psi^3) \cdots (\phi_k^1 \psi^2) \phi_i^1 \phi_j^2 \phi_k^1 \psi^2 \psi^3 \cdots \psi_n^0 \quad \text{by rules one and two, into the final state} \]

---

where the memory sequence element \(x_i^r\) characterizes the \(l\)th eigenfunction, \(P_r^r\) of the operator \(A^r\). Again, the square amplitude measure for each element of the superposition (2.12) reduces to the product measure of the individual memory elements, \(|(\phi_i^r \psi^{S_i})|^2\) for the memory sequence element \(x_i^r\). Therefore, the memory sequence of a typical sequence of (2.12) has all the characteristics of a random sequence, with individual, independent (and now different), probabilities \(|(\phi_i^r \psi^{S_i})|^2\) for the \(r\)th memory state. 

Finally, we can generalize to the case where neural observations are allowed to be performed upon the count system.

---

For example, if we permit the observation of a new quantity \(B\), (eigenfunctions \(\{n_j\}\) memory characterization \(p_j\)) upon the system \(S_r\) for which \(A^r\) has already been observed, then the state (2.12): 

\[ (2.13) \quad \psi = \sum_{i_1, i_2, \dots, i_n} (\phi_i^1 \psi^s) \dots (\phi_i^r \psi^s) \dots (\phi_i^m \psi^s) \phi_i^1 \phi_i^2 \dots \phi_i^r \psi^s \tag{2.13} \]

is transformed by rule two into the state: 

\[ (2.14) \quad \psi = \sum_{i_1, i_2, \dots} (\phi_i^1 \psi^s) \dots (\phi_i^r \phi_i^s) \dots (\phi_i^m \psi^s) (\phi_i^1 \phi_i^2 \dots \phi_i^r \phi_i^s) \phi_i^1 \phi_i^2 \dots \phi_i^m \psi^s \tag{2.14} \]

The relative system states for \(S_r\) have been changed from the eigenstates of \(A^r\), \(\{p_j^s\}\), to the eigenstates of \(B_j\), \(\{n_j^m\}\). We notice further that, with respect to our measure on the superposition, the memory sequence state have the character of random sequences, but is of random sequences for which the individual times are no longer independent. The memory states \(\beta^m\) now depend upon the memory states \(\alpha^r\) which represent the result of the previous measurement upon the same system, \(S_r\). The joint measure for this pair of memory states, conditional by field values for remaining memory states is: 

\[ (2.15) \quad M \propto \frac{\alpha_1^1 \dots \alpha_{r_1}^{r_1} \dots \alpha_m^m}{\alpha_1^1 \dots \alpha_{r_1}^{r_1} \alpha_m^m} \propto \frac{M(\alpha_1^1 \dots \alpha_{r_1}^{r_1} \cdots \alpha_m^m \phi_1^1 \phi_2^1 \cdots \phi_m^m)}{\sum_{m} M(\alpha_1^1 \dots \alpha_{r_1}^{r_1} m \phi_1^1 \phi_2^1 \cdots \phi_m^m)} \tag{2.15} \]

\[ = \frac{|(\phi_i^1 \psi^s) \dots (\phi_i^r \psi^r) \dots (\phi_i^m \psi^s) (\phi_i^1 \psi^s) (\phi_i^2 \psi^s) \dots (\phi_i^m \psi^s) (\phi_i^m \psi^s) (\phi_i^m \psi^s)|^2}{\sum_{m} |(\phi_i^1 \psi^s) \dots (\phi_i^r \psi^{r_1}) \dots (\phi_i^m \psi^s) (\phi_i^m \psi^{r_1})|^2} = |(\phi_i^1 \psi^s)|^2 (\phi_i^r \psi^s)|^2 \tag{2.15} \]

---

The joint measure (2015) is first of all, independent
of the memory states for the remaining systems (S₁, S₂, etc.).
Second, the dependence of \(B_m^r\) on \(\alpha_r\) is equivalent,
measure theoretically, to that given by the stochastic
process² which converts the states of \(r\) into the
states \(N_m^r\) with transition probabilities: 

\[ (2.16) \quad T_{em} = \text{Prob}(\phi_r^r \to N_m^r) = |\langle \phi_r^r, \phi_r^r \rangle|^2 \]

If we were to allow yet another quantity, \(C\),
be measured in \(S_r\), the new memory states \(\phi_r^r\)
correspondingly to the eigenfunctions of \(C\) would have
a similar dependence upon the previous states \(B_m^r\), but
no direct dependence on the still earlier states \(\alpha_r\). This
dependence upon only the previous result of observation is
a consequence of the fact that the relative system
states are completely determined by the last observation. 

We can therefore summarize the situation
for an arbitrary sequence of observations, upon the
same or different systems in any order, and for
which the number of observations of each quantity in
each system is very large, with the following result:

---

of memory sequence.
Except for a set of memory memory zero, the
average of any function of our a memory sequence
can be calculated approximately by the use of
the independent probabilities given by Proves 1 for each
initial observation on a system, and by the use of
the transition probabilities (2.16) for successively observation
upon the same system. In this limit as the number
of all types of observations goes to infinity the calculation is
exact, and the expectation set has measure zero.
This prescription for the calculation of average over
memory sequence by probabilities assigned to
individual elements is previously that of the
orthodox theory (Proves 1). We have therefore shown that
all predictions of the usual theory will appear to
be valid to almost all observers, since their
predictions hold for almost all memory sequence.
In particular, the uncertainty principle is
never violated, since, at about, the latest
measurement upon a system supplies all possible
information about the relative system state, so that there
is no direct correlation between any earlier results of
observation on the system, and the succeeding observation.
Any observation of a quantity B, between two
successive observations of quantity A (call on this
same system) will destroy the one-one correspondence
between the earlier and later memory states for
the result of A. Thus for arbitrarily observations
of different quantities there are fundamental limitations
upon the correlations between memory states for
the same observed quantity, these limitations
expiring the content of the uncertainty principle.

---

In conclusion, in this section, we have described the probability of a disorder, a process which is entirely deterministic and continuous from the actual event point (presumed to be a binary equation at all times) that occurs at most once or a superposition, each element of which describes an element with differing memory rates. We have shown that the probability aspects of this event form of quantum theory are valid, and we have seen how pure wave mechanics, without any initial probability assertions can lead to this notion on a subjective level, or appearances to observers.