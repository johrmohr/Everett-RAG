# Everett Handwritten Draft -- 5 Prediction Schemes circa 1955.pdf

So far we have discussed a unique
containing observer only upon the basis of
wave mechanics, by analyzing state functions
in all systems, in which we observe, and
considering their interactions. This is the basis
of the complete theory. However, questions arise
as to the amount of knowledge an observer
can obtain about the state of another physical
system — that is, may be more or less complete,
for an observer to obtain this complete state
function for some other observer, without
an instrument to monitor it or completely know
the functionality of the second observer. Actually,
however, it is not really relevant that
an observer should be interested in the
state function themselves, but only in
really which will correlate his experience.
Thus what one is really interested in
is functions of one's memory which have
the purpose of useful prediction. This problem
can be formally by solving functions
of memory arguments [—], [...] which best
predict future events of the sequence.
Thus such questions concerning the knowledge
available to observers, and any limitations
on it can be investigated within the
prominence of our formal theory. This
involves within a note on one state the wave
function for the whole universe, which is considered

---

at the basic element of our theory. The theory simply provides a logical formula from which deductions can be made, which can be put into correspondence with our experience. The theory is even capable of describing any limitations upon actual knowledge available to observers. -- it tells us which deductions can be put into such correspondence, namely its predictions about appearance to observers.) 

Example of such
deduction
of limitations 

Uncertainty Principle 

Formalize process where Observer first
measures portion, then moment. Investigate
relation of memory sequence. 2e
show that for no ratio of system is it
possible their memory sequence contain unlimited
repeatable portion. Monitor measurement. 

ie general limitations on predictions for
non-community quantities.

---

Finally, we allow a single observer to perform a sequence of different measurements upon the same system. Let him first observe A, not the weight 

\[W^{s=0} = \sum_i a_i b_i \gamma_i^0 [\alpha_i]\]

Let him now measure B, so that weight 

\[\sum_{ij} a_i b_{ij} \gamma_i \gamma_j^0 [\alpha_i \beta_j]\]

Let him finally re-measure A, which weight 

\[\sum_{ijkl} a_i b_{ij} b_{kl} \phi_{kj} \phi_{kl} \gamma_i^0 [\alpha_i \beta_j \alpha_k]\]

and just as before 

and so forth, for a long sequence 

We notice furthermore, that under the overall
applies equally well to successive measurements upon
systems of different states, a well or measurement
of different quantities in the same system. And all
cases our measure leads to a measure on the
individual memory sequence almost which is equivalent
to the sum of the usual probability for the result.

---

Shu, if we wish, we can regard observation at a 

proves which converts a single observer 

with memory sequence \([A, B, \ldots, C]\) into a number
of observers with memory sequence \([A, B, \ldots, C, D_1, D_2]\) 

There is no question about which of the final
observers corresponds to the initial one, since
each of them powers the total memory of the first.
(Which one is the original one?) 

The successive memory sequences of an observer
then do not form a linear array, but a
planar graph (tree): 

\[
\begin{bmatrix}
A_1 B_1 C_1 \\
A_2 B_2 \\
A_3 B_3 \\
\vdots
\end{bmatrix}
=
\begin{bmatrix}
A_1 B_1 C_1 \\
A_2 B_2 C_2 \\
A_3 B_3 C_3 \\
\vdots
\end{bmatrix}
\]

that is, the trajectory of an observer forms
not a line but such a tree.

---

We consider the case of a single linear performing
a series of different measurements upon the same system.
Let him first measure \( A \), then we get 

\[ \sum a_i \psi_i^0 \frac{Y_0^0}{Z_0^0} \]

Let him now measure \( B \) 

\[ \mathcal{B} = \sum_{i} b_{ij} \mathcal{N}_j \]

\[ \Rightarrow \sum_{ij} a_i b_{ij} \mathcal{N}_j \frac{Y_0^0}{Z_0^0} \]

Then we now see that condition of the
memory sequence. 

Se in general, an observer in
state \( \psi = 0 \) can not predict results
\( \{\text{---}\} \) because later in his memory
of future measurements any later than those
given by his relative state function (system), since
the future memory state (configuration) are
completely determined by this.

---

We see that the prediction of an observer has fundamental limitations. These limitations arise, however, not from the fact that there is no unique correspondence from initial to final system states, but because there is no unique final observer state for a given initial state. 

As far as the theory is concerned all elements of the superposition exist simultaneously, neither is no question of any partition observer described by an element of the superposition being any more "real" than another. Thus, when an observer, state \(W_0\) make observation, no that there will be a superposition 

\[ \sum_{i} a_i \psi_i \]

each observer described by an element of the superposition (limited by state \(W_0\)) has regarded as existing independently of the rest. That is, for all future interactions each such element independently obeys the wave equation, ie proceeds in exactly the same manner as if the total content of the single observer \(W_0\) (rule 2) The presence or absence of the remaining observer state in no way affects the future behavior of the observer described by \(W_0\) so that it appears to think of it as an independently existing observer.